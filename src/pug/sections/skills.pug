section#skills
    h2
        include ../../svg/icons/skills.svg
        = " "
        | Compétences

    h3 Langages
    dl
        dt Expert javascript
        dd.
            ES5, #[strong ES2015+]#[br/]
            #[strong node.js] (server, CLI, tools)#[br/]
            #[strong React] / #[strong Flux] / #[strong Redux] / #[strong MobX], Angular, Ember, Vue.js#[br/]
            #[strong #[abbr( title="Document Object Model" ) DOM]], APIs HTML5 (canvas, geolocalisation, #[abbr( title="Server-Sent Events" ) SSE], storage, …)#[br/]
            jQuery, AJAX/AJAJ, lodash, CoffeeScript
        dt.
            Spécialiste standards #[abbr( title="World Wide Web Consortium" ) W3C] / #[abbr( title="Web Hypertext Application Technology Working Group" ) WHATWG]
        dd.
            #[strong #[abbr( title="Hypertext Markup Language" ) html] 5],
            #[abbr( title="eXtended Hypertext Markup Language" ) xhtml],
            #[strong #[abbr( title="Cascading Stylesheet" ) css]],
            #[abbr( title="eXtended Markup Language" ) xml],
            #[abbr( title="eXtensible Stylesheet Language" ) xsl], xpath,
            #[abbr( title="Web Accessibility Initiative" ) wai],
            #[abbr( title="Scalable Vector Graphics" ) svg]
        dt Spécialiste performances web
        dd server-side, client-side
        dt Front-end
        dd #[strong html5], xhtml, #[strong css3], #[strong stylus], sass, #[strong javascript], json, xml, xsl(t), #[strong RegExp]
        dt Back-end
        dd #[strong node.js] (express, sockets.io, …), NoSQL (#[strong Redis], MongoDB, CouchDB), #[strong GraphQL], #[strong ArangoDB], MySQL, #[strong PostgreSQL], ElasticSearch, PHP
        dt Application
        dd iOS natif (Swift)
        dt Système
        dd bash, python, go-lang

    h3 Outils &amp; Méthodologies
    dl
        dt Adobe Creative Suite CC
        dd photoshop, illustrator, indesign, flash, edge tools
        dt Apple Pro Apps
        dd Logic Pro 8, Final Cut Pro X, XCode 4+
        dt Text Editors &amp; IDE
        dd Vim, #[strong neovim], Atom, VSCode, Sublime Text 2 &amp; 3
        dt Task runners and workflow control
        dd #[strong Webpack], GruntJS, GulpJS, Codekit, Vagrant, #[strong docker], TravisCI, Jenkins
        dt Virtualisation, containers &amp; provisioning
        dd #[strong docker], Vagrant, Chef, Puppet, #[strong AWS lambda]
        dt Version Control
        dd.
            #[strong git],
            #[abbr( title="Subversion" ) SVN],
            #[abbr( title="Mercurial" ) hg]
        dt Méthodologies
        dd.
            Scrum,
            #[abbr( title="Get Things Done" ) GTD],
            #[abbr( title="eXtreme Programming" ) XP]

    h3 Systèmes
    dl
        dt Mac OSX
        dd 10.6~11, 10.12 (système principal)
        dt Linux
        dd debian-based, shell (bash/zsh, automation)
        dt Administration Système, DevOps
        dd #[strong nginx], apache, #[strong AWS] (ECS/ECR, lambda)
